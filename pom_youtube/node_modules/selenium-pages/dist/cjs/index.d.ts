import { Builder, WebDriver } from "selenium-webdriver";
import { Pages, SelenOptions } from "./pages/";
export { Pages, SelenOptions, SelenOptions as PageOptions, WebElementEx, Dictionary } from "./pages/";
export declare class Selen {
    static Builder: typeof Builder;
    static Pages: typeof Pages;
    static addPath(path: string): void;
    static build(browser: string): Promise<WebDriver>;
}
export declare namespace Selen {
    type Options = SelenOptions;
}
